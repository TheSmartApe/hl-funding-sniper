# Copy this file to .env (or export these in your shell) and fill in values.
# NEVER commit .env. It is gitignored.

# Required only when execution.dry_run = false in config.yaml
HL_PRIVATE_KEY=

# Required only when notifications.telegram.enabled = true
TELEGRAM_BOT_TOKEN=
